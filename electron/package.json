{
  "name": "email_client",
  "version": "1.0.0",
  "description": "A lightweight email client for sending and reading emails using AWS",
  "license": "MIT",
  "author": "Zack Young",
  "type": "commonjs",
  "main": "main.js",
  "scripts": {
    "sass:compile": "sass scss/main.scss assets/css/styles.css",
    "sass:prod": "sass --no-source-map --style=compressed scss/main.scss assets/css/styles.css",
    "start": "electron --no-sandbox .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "bootstrap": "^5.3.7",
    "electron": "^37.2.4"
  }
}
